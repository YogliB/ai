{
	"description": "Inject slashkit rules into subagent prompts.",
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Task",
				"hooks": [
					{
						"type": "command",
						"command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/inject-rules.py"
					}
				]
			}
		]
	}
}
